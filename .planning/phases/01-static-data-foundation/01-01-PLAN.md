---
phase: 01-static-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/bmr.ts
autonomous: true
requirements:
  - BMR-01
  - BMR-02
  - BMR-03
  - BMR-04
  - BMR-05
  - BMR-06

must_haves:
  truths:
    - "calculateBMR(30, 'male', 175, 70) returns 1648.75 (Mifflin-St Jeor, no rounding)"
    - "calculateBMR(30, 'female', 175, 70) returns exactly 166 kcal less than male (1482.75)"
    - "calculateTDEE(1648.75, 1.2) returns 1980 (rounded to nearest 10)"
    - "ACTIVITY_LEVELS array has exactly 5 entries with multipliers 1.2, 1.375, 1.55, 1.725, 1.9"
    - "UserProfile, BMRResult interfaces and ActivityLevelId type are exported from types.ts"
    - "tsc -b && vite build passes with zero errors"
  artifacts:
    - path: "src/data/bmr.ts"
      provides: "calculateBMR, calculateTDEE, ACTIVITY_LEVELS exports"
      exports: ["calculateBMR", "calculateTDEE", "ACTIVITY_LEVELS", "ActivityLevelId"]
    - path: "src/data/types.ts"
      provides: "UserProfile, BMRResult, ActivityLevelId type additions"
      contains: "UserProfile"
  key_links:
    - from: "src/data/bmr.ts"
      to: "src/data/types.ts"
      via: "import type { UserProfile, BMRResult, ActivityLevelId }"
      pattern: "import type.*types"
---

<objective>
Create the BMR/TDEE pure calculation module and extend the types file with all new BMR-related interfaces.

Purpose: Downstream phases (Phase 2 SettingsService, Phase 4 Settings UI) need importable, type-safe BMR functions. These are leaf modules with zero runtime dependencies.
Output: src/data/bmr.ts (new) and src/data/types.ts (extended with BMR types).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-static-data-foundation/01-CONTEXT.md
@.planning/phases/01-static-data-foundation/01-RESEARCH.md

<interfaces>
<!-- Existing patterns from src/data/types.ts that new types must follow -->
<!-- Named exports only. Interfaces are PascalCase. Constants are UPPER_SNAKE_CASE. -->
<!-- Section dividers: // ── Section Name ── pattern -->
<!-- File-level block comment with ASCII art header required on new files -->

From src/data/types.ts (append after line 210, before EOF):
```typescript
// ── BMR / TDEE Types ────────────────────────────────

/** 使用者基本資料，用於 BMR 計算 */
export interface UserProfile {
  ageYears: number;
  sex: "male" | "female";
  heightCm: number;
  weightKg: number;
  activityLevelId: ActivityLevelId;
}

/** BMR + TDEE 計算結果 */
export interface BMRResult {
  /** 基礎代謝率 kcal/day（未四捨五入） */
  bmr: number;
  /** 每日總消耗熱量 kcal/day（四捨五入至十位數） */
  tdee: number;
}
```

ActivityLevelId is derived from ACTIVITY_LEVELS in bmr.ts:
```typescript
export type ActivityLevelId = (typeof ACTIVITY_LEVELS)[number]["id"];
```
Note: ActivityLevelId must be exported from bmr.ts (not types.ts) because it is derived
from the ACTIVITY_LEVELS const. Then UserProfile in types.ts imports it from bmr.ts.
Alternative (simpler, avoids circular imports): define ActivityLevelId as an explicit
string union in types.ts and export it from there so both types.ts and bmr.ts stay
independent. Use this approach:
```typescript
// In types.ts:
export type ActivityLevelId =
  | "sedentary"
  | "light"
  | "moderate"
  | "very"
  | "extra";
```
Then in bmr.ts, import ActivityLevelId from "./types" and use `as const` on ACTIVITY_LEVELS
ensuring the id literals match the union exactly.

From src/data/foods.ts (file header pattern):
```typescript
/**
 * ============================================================
 * [Module Title in Chinese]
 * ============================================================
 *
 * [One-line description]
 *
 * [Formula citation if applicable]
 */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types.ts with BMR and activity level type definitions</name>
  <files>src/data/types.ts</files>
  <read_first>
    - src/data/types.ts — read fully before editing; append new section after the last existing export (WeightEntry, SupplementLogEntry section)
  </read_first>
  <action>
Append the following block to the END of src/data/types.ts, after the SupplementLogEntry interface. Do not modify any existing content.

```typescript
// ── BMR / TDEE Types ────────────────────────────────

/** 活動量等級 ID — 對應 bmr.ts 的 ACTIVITY_LEVELS 陣列 */
export type ActivityLevelId =
  | "sedentary"
  | "light"
  | "moderate"
  | "very"
  | "extra";

/** 使用者基本資料，用於 BMR 計算 */
export interface UserProfile {
  ageYears: number;
  sex: "male" | "female";
  /** 身高（公分） */
  heightCm: number;
  /** 體重（公斤） */
  weightKg: number;
  activityLevelId: ActivityLevelId;
}

/** BMR + TDEE 計算結果 */
export interface BMRResult {
  /** 基礎代謝率 kcal/day（未四捨五入） */
  bmr: number;
  /** 每日總消耗熱量 kcal/day（四捨五入至十位數） */
  tdee: number;
}

// ── Dietary Guideline Types ─────────────────────────

/** 三大營養素佔總熱量的百分比 */
export interface MacroRatios {
  /** % of TDEE */
  protein: number;
  /** % of TDEE */
  fat: number;
  /** % of TDEE */
  carb: number;
}

/** 三大營養素的克數目標 */
export interface MacroGrams {
  protein: number;
  fat: number;
  carb: number;
}

/** 飲食指南預設組 */
export interface GuidelinePreset {
  id: string;
  /** 顯示名稱（繁體中文） */
  name: string;
  /** 發布機構（繁體中文） */
  authority: string;
  /** 來源 URL */
  sourceUrl: string;
  /** 版本年份 */
  year: number;
  /**
   * 三大營養素佔總熱量百分比。
   * 三項相加應為 100。
   * 儲存百分比（非克數）— 克數由 calculateMacroGrams() 動態計算。
   */
  macroRatios: MacroRatios;
}
```

Exact placement: append after line 210 (after the closing `}` of SupplementLogEntry), with one blank line separator. Follow 2-space indentation and double-quote strings per project convention.

NOTE: The dietary guideline types (MacroRatios, MacroGrams, GuidelinePreset) are added here in the same task to keep all type definitions co-located in types.ts, per the established project pattern. Plan 02 will use them.
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ActivityLevelId" src/data/types.ts` returns a line with the union type definition
    - `grep -n "UserProfile" src/data/types.ts` returns the interface definition
    - `grep -n "BMRResult" src/data/types.ts` returns the interface definition
    - `grep -n "GuidelinePreset" src/data/types.ts` returns the interface definition
    - `grep -n "MacroGrams" src/data/types.ts` returns the interface definition
    - `npx tsc --noEmit` exits with code 0 (no TypeScript errors)
    - All new exports use named export syntax (no `export default`)
  </acceptance_criteria>
  <done>types.ts compiles cleanly and exports ActivityLevelId, UserProfile, BMRResult, MacroRatios, MacroGrams, and GuidelinePreset as named exports.</done>
</task>

<task type="auto">
  <name>Task 2: Create src/data/bmr.ts with Mifflin-St Jeor BMR/TDEE functions and ACTIVITY_LEVELS</name>
  <files>src/data/bmr.ts</files>
  <read_first>
    - src/data/foods.ts — read first 15 lines for file header pattern to replicate exactly
    - src/data/types.ts — confirm ActivityLevelId union values match ACTIVITY_LEVELS id literals
  </read_first>
  <action>
Create src/data/bmr.ts as a new file with the following exact content.

CRITICAL — BMR formula constants:
- Male offset: +5
- Female offset: -161
- Reference verification: calculateBMR(30, "male", 175, 70) must equal 1648.75
  Calculation: (10 × 70) + (6.25 × 175) - (5 × 30) + 5 = 700 + 1093.75 - 150 + 5 = 1648.75
- The value 1673.75 cited in older planning docs is INCORRECT. Use 1648.75.

CRITICAL — TDEE rounding:
- Multiply FIRST, then round: Math.round(bmr * multiplier / 10) * 10
- Do NOT round bmr first and then multiply.

CRITICAL — No enum keyword. Use `as const` array with derived type or explicit union. The
ActivityLevelId union in types.ts must exactly match the id literals in ACTIVITY_LEVELS.

File content:

```typescript
/**
 * ============================================================
 * BMR / TDEE 計算
 * ============================================================
 *
 * 純函式，無副作用，無 I/O。
 * 所有公式採用公制單位（公斤、公分）。
 *
 * 公式來源：Mifflin & St Jeor (1990)
 * doi:10.1093/ajcn/51.2.241
 *
 * 驗證值：30歲男性，70kg，175cm → BMR = 1648.75 kcal/day
 */

import type { ActivityLevelId, BMRResult } from "./types";

// ── Activity Levels ──────────────────────────────────

/**
 * 活動量等級，含乘數與繁體中文標籤。
 * 乘數來源：Harris & Benedict 修正版，廣泛用於 TDEE 估算。
 */
export const ACTIVITY_LEVELS = [
  {
    id: "sedentary" as const,
    label: "久坐",
    description: "坐辦公室，每週運動 0–1 次",
    multiplier: 1.2,
  },
  {
    id: "light" as const,
    label: "輕度活動",
    description: "每週輕度運動 1–3 天",
    multiplier: 1.375,
  },
  {
    id: "moderate" as const,
    label: "中度活動",
    description: "每週中度運動 3–5 天",
    multiplier: 1.55,
  },
  {
    id: "very" as const,
    label: "高度活動",
    description: "每週運動 6–7 天",
    multiplier: 1.725,
  },
  {
    id: "extra" as const,
    label: "極高活動",
    description: "體力勞動工作或每日高強度訓練",
    multiplier: 1.9,
  },
] as const;

/** 活動量等級查找 Map，O(1) by id */
export const ACTIVITY_LEVEL_MAP = new Map<ActivityLevelId, (typeof ACTIVITY_LEVELS)[number]>();
ACTIVITY_LEVELS.forEach((a) => ACTIVITY_LEVEL_MAP.set(a.id, a));

// ── BMR Calculation ──────────────────────────────────

/**
 * 計算基礎代謝率（BMR）。
 *
 * 使用 Mifflin-St Jeor 公式（1990）：
 *   男性：10W + 6.25H - 5A + 5
 *   女性：10W + 6.25H - 5A - 161
 *
 * 回傳值為未四捨五入的 kcal/day。
 * 驗證：calculateBMR(30, "male", 175, 70) === 1648.75
 *
 * @param ageYears - 年齡（歲），有效範圍 10–120
 * @param sex - 性別 "male" | "female"
 * @param heightCm - 身高（公分），有效範圍 100–250
 * @param weightKg - 體重（公斤），有效範圍 30–300
 */
export function calculateBMR(
  ageYears: number,
  sex: "male" | "female",
  heightCm: number,
  weightKg: number,
): number {
  // Mifflin-St Jeor (1990): doi:10.1093/ajcn/51.2.241
  // IMPORTANT: male offset is +5, female offset is -161.
  // Swapping these produces a 166 kcal error on every output.
  const GENDER_OFFSET = sex === "male" ? 5 : -161;
  return 10 * weightKg + 6.25 * heightCm - 5 * ageYears + GENDER_OFFSET;
}

/**
 * 計算每日總消耗熱量（TDEE）。
 *
 * 回傳值四捨五入至十位數 kcal/day。
 * 先乘後捨：Math.round(bmr * multiplier / 10) * 10
 *
 * @param bmr - calculateBMR() 的回傳值（未四捨五入）
 * @param activityMultiplier - ACTIVITY_LEVELS 中對應的 multiplier 值
 */
export function calculateTDEE(bmr: number, activityMultiplier: number): number {
  return Math.round((bmr * activityMultiplier) / 10) * 10;
}

/**
 * 給定 UserProfile 中的 activityLevelId，回傳對應的乘數。
 * 若 id 不存在（不應發生），回傳久坐乘數 1.2 作為安全預設值。
 */
export function getActivityMultiplier(id: ActivityLevelId): number {
  return ACTIVITY_LEVEL_MAP.get(id)?.multiplier ?? 1.2;
}

/**
 * 一次性計算 BMR + TDEE 並回傳 BMRResult。
 *
 * @param ageYears - 年齡（歲）
 * @param sex - 性別
 * @param heightCm - 身高（公分）
 * @param weightKg - 體重（公斤）
 * @param activityLevelId - 活動量等級 ID
 */
export function calculateBMRResult(
  ageYears: number,
  sex: "male" | "female",
  heightCm: number,
  weightKg: number,
  activityLevelId: ActivityLevelId,
): BMRResult {
  const bmr = calculateBMR(ageYears, sex, heightCm, weightKg);
  const multiplier = getActivityMultiplier(activityLevelId);
  return { bmr, tdee: calculateTDEE(bmr, multiplier) };
}
```

Verify the file follows:
- 2-space indentation
- Double quotes for all strings
- Trailing commas in arrays and objects
- Named exports only (no default export)
- Relative import path "./types" (not "@/data/types")
- File-level block comment with ASCII art header
- Section dividers using `// ── Name ──` pattern
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "calculateBMR" src/data/bmr.ts` returns the function export
    - `grep -n "calculateTDEE" src/data/bmr.ts` returns the function export
    - `grep -n "ACTIVITY_LEVELS" src/data/bmr.ts` returns the const export
    - `grep -n "ACTIVITY_LEVEL_MAP" src/data/bmr.ts` returns the Map export
    - `grep -n "1648.75" src/data/bmr.ts` returns at least one line (JSDoc reference value)
    - `grep "1673.75" src/data/bmr.ts` returns nothing (wrong value must NOT appear)
    - `grep -n "enum" src/data/bmr.ts` returns nothing (no TypeScript enums used)
    - `grep -n "export default" src/data/bmr.ts` returns nothing
    - `grep -n "@/" src/data/bmr.ts` returns nothing (no path alias used)
    - `npx tsc --noEmit` exits with code 0
    - Manual spot-check: (10*70) + (6.25*175) - (5*30) + 5 = 700 + 1093.75 - 150 + 5 = 1648.75 ✓
    - ACTIVITY_LEVELS has exactly 5 entries (sedentary, light, moderate, very, extra)
    - All 5 id literals match the ActivityLevelId union in types.ts exactly
  </acceptance_criteria>
  <done>src/data/bmr.ts exports calculateBMR, calculateTDEE, calculateBMRResult, getActivityMultiplier, ACTIVITY_LEVELS, ACTIVITY_LEVEL_MAP. TypeScript strict mode compiles cleanly. Reference BMR value is 1648.75 (not 1673.75).</done>
</task>

</tasks>

<verification>
After both tasks:
- `cd /home/ubuntu/works/eat-manager && npx tsc --noEmit` — zero errors
- `grep -n "UserProfile\|BMRResult\|ActivityLevelId\|GuidelinePreset\|MacroRatios\|MacroGrams" src/data/types.ts` — all 6 names present
- `grep -c "as const" src/data/bmr.ts` — at least 5 occurrences (one per activity level id)
- `grep -n "1648.75" src/data/bmr.ts` — reference value present in JSDoc
</verification>

<success_criteria>
- types.ts exports ActivityLevelId, UserProfile, BMRResult, MacroRatios, MacroGrams, GuidelinePreset
- bmr.ts exports calculateBMR, calculateTDEE, calculateBMRResult, getActivityMultiplier, ACTIVITY_LEVELS, ACTIVITY_LEVEL_MAP
- calculateBMR(30, "male", 175, 70) = 1648.75 (verifiable by reading the formula and constants)
- calculateTDEE uses multiply-first-then-round formula
- ACTIVITY_LEVELS has 5 entries matching multipliers 1.2 / 1.375 / 1.55 / 1.725 / 1.9
- npx tsc --noEmit passes with zero errors
- No default exports, no enum keyword, no @/ path alias, no new npm packages
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-data-foundation/01-01-SUMMARY.md`
</output>
