---
phase: 01-static-data-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/data/dietary-guidelines.ts
autonomous: true
requirements:
  - DIET-01
  - DIET-02
  - DIET-03
  - DIET-04
  - DIET-05
  - DIET-06
  - DIET-07

must_haves:
  truths:
    - "GUIDELINES array contains exactly 3 presets (taiwan-hpa, usda-amdr, japan-mhlw)"
    - "Each preset has id, name (zh-TW), authority (zh-TW), sourceUrl, year, and macroRatios"
    - "Each preset's macroRatios.protein + macroRatios.fat + macroRatios.carb === 100"
    - "calculateMacroGrams(2000, usda_amdr_preset) returns { protein: 100, fat: 67, carb: 250 }"
    - "GUIDELINE_MAP provides O(1) lookup by id for all 3 presets"
    - "tsc -b && vite build passes with zero errors"
  artifacts:
    - path: "src/data/dietary-guidelines.ts"
      provides: "GUIDELINES array, GUIDELINE_MAP, calculateMacroGrams function"
      exports: ["GUIDELINES", "GUIDELINE_MAP", "calculateMacroGrams"]
  key_links:
    - from: "src/data/dietary-guidelines.ts"
      to: "src/data/types.ts"
      via: "import type { GuidelinePreset, MacroGrams, MacroRatios }"
      pattern: "import type.*types"
---

<objective>
Create the dietary guidelines catalog module with 3 national presets and a macro gram calculation function.

Purpose: Phase 4 Settings UI needs an importable catalog of guideline presets to display to users. Phase 2 SettingsService needs calculateMacroGrams() to compute personalized targets. These are pure data and arithmetic — no I/O, no side effects.
Output: src/data/dietary-guidelines.ts (new file).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-static-data-foundation/01-CONTEXT.md
@.planning/phases/01-static-data-foundation/01-RESEARCH.md
@.planning/phases/01-static-data-foundation/01-01-SUMMARY.md

<interfaces>
<!-- Types defined in Plan 01 (src/data/types.ts) that this file uses -->

From src/data/types.ts (added in Plan 01):
```typescript
export interface MacroRatios {
  protein: number; // % of TDEE
  fat: number;     // % of TDEE
  carb: number;    // % of TDEE
}

export interface MacroGrams {
  protein: number;
  fat: number;
  carb: number;
}

export interface GuidelinePreset {
  id: string;
  name: string;        // zh-TW display name
  authority: string;   // zh-TW issuing authority
  sourceUrl: string;
  year: number;
  macroRatios: MacroRatios; // percentages summing to 100
}
```

Guideline values (from RESEARCH.md):
- Taiwan HPA DRI: protein=12%, fat=25%, carb=63% — MEDIUM confidence (secondary sources)
  id: "taiwan-hpa", year: 2011
  sourceUrl: "https://www.hpa.gov.tw/Pages/Detail.aspx?nodeid=4248&pid=12285"
- USDA AMDR: protein=20%, fat=30%, carb=50% — HIGH confidence
  id: "usda-amdr", year: 2025
  sourceUrl: "https://www.dietaryguidelines.gov/"
- Japan MHLW DRI 2025: protein=16%, fat=25%, carb=59% — HIGH confidence
  id: "japan-mhlw", year: 2025
  sourceUrl: "https://www.mhlw.go.jp/content/001151422.pdf"

calculateMacroGrams verification (USDA, TDEE=2000):
  protein: round(2000 * 20/100 / 4) = round(100) = 100g
  fat:     round(2000 * 30/100 / 9) = round(66.67) = 67g
  carb:    round(2000 * 50/100 / 4) = round(250) = 250g
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/data/dietary-guidelines.ts with GUIDELINES catalog and calculateMacroGrams</name>
  <files>src/data/dietary-guidelines.ts</files>
  <read_first>
    - src/data/foods.ts — read first 15 lines for the exact file header comment pattern to replicate
    - src/data/types.ts — confirm GuidelinePreset, MacroGrams, MacroRatios are exported (added by Plan 01)
    - src/data/remedies.ts — read first 20 lines for the array + Map export pattern
  </read_first>
  <action>
Create src/data/dietary-guidelines.ts as a new file. Follow the exact same file structure as foods.ts:
file-level block comment → imports → section divider → data array → Map → functions.

File content:

```typescript
/**
 * ============================================================
 * 飲食指南預設組
 * ============================================================
 *
 * 收錄三個國家級飲食指南的三大營養素建議比例。
 * 純資料模組，無 I/O，無副作用。
 *
 * 來源：
 *   台灣  — 衛生福利部國民健康署 第八版國人膳食營養素參考攝取量 (DRI)
 *   美國  — USDA Dietary Guidelines for Americans 2025–2030 (AMDR)
 *   日本  — 厚生勞動省 日本人の食事摂取基準 2025年版
 */

import type { GuidelinePreset, MacroGrams } from "./types";

// ── Guideline Presets ───────────────────────────────

/**
 * 三個國家/機構的飲食指南預設組。
 * macroRatios 儲存佔 TDEE 的百分比（非克數），三項相加為 100。
 * 克數由 calculateMacroGrams() 動態計算，避免硬編碼個人化值。
 */
export const GUIDELINES: GuidelinePreset[] = [
  {
    id: "taiwan-hpa",
    name: "台灣衛福部 DRI",
    authority: "衛生福利部國民健康署",
    sourceUrl: "https://www.hpa.gov.tw/Pages/Detail.aspx?nodeid=4248&pid=12285",
    year: 2011,
    macroRatios: {
      // 第八版 DRI — 中信度（來自二手資料，待比對中文原始 PDF 確認）
      // STATE.md 已登錄為已知疑慮：建議在 v1.1 對照原始文件驗證
      protein: 12,
      fat: 25,
      carb: 63,
    },
  },
  {
    id: "usda-amdr",
    name: "美國 USDA AMDR",
    authority: "美國農業部（USDA）",
    sourceUrl: "https://www.dietaryguidelines.gov/",
    year: 2025,
    macroRatios: {
      // AMDR 中間值：蛋白質 10–35%、脂肪 20–35%、碳水 45–65%
      // 高信度 — 來自 Dietary Guidelines for Americans 2025–2030 官方文件
      protein: 20,
      fat: 30,
      carb: 50,
    },
  },
  {
    id: "japan-mhlw",
    name: "日本厚生勞動省 DRI",
    authority: "日本厚生勞動省",
    sourceUrl: "https://www.mhlw.go.jp/content/001151422.pdf",
    year: 2025,
    macroRatios: {
      // 2025 年版中間值：蛋白質 13–20%、脂肪 20–30%、碳水 50–65%
      // 高信度 — 來自日本人の食事摂取基準 2025 年版官方 PDF
      protein: 16,
      fat: 25,
      carb: 59,
    },
  },
];

/** 飲食指南 ID 查找 Map，O(1) by id */
export const GUIDELINE_MAP = new Map<string, GuidelinePreset>();
GUIDELINES.forEach((g) => GUIDELINE_MAP.set(g.id, g));

// ── Macro Calculation ────────────────────────────────

/**
 * 根據 TDEE 和飲食指南預設組，計算三大營養素克數目標。
 *
 * 換算公式：
 *   蛋白質、碳水：1g = 4 kcal
 *   脂肪：1g = 9 kcal
 *
 * 範例（USDA AMDR，TDEE 2000 kcal）：
 *   蛋白質：round(2000 × 0.20 / 4) = 100g
 *   脂肪：  round(2000 × 0.30 / 9) ≈ 67g
 *   碳水：  round(2000 × 0.50 / 4) = 250g
 *
 * @param tdeeKcal - 每日總消耗熱量（kcal/day），由 calculateTDEE() 取得
 * @param preset - 選定的飲食指南預設組
 */
export function calculateMacroGrams(tdeeKcal: number, preset: GuidelinePreset): MacroGrams {
  return {
    protein: Math.round((tdeeKcal * preset.macroRatios.protein) / 100 / 4),
    fat: Math.round((tdeeKcal * preset.macroRatios.fat) / 100 / 9),
    carb: Math.round((tdeeKcal * preset.macroRatios.carb) / 100 / 4),
  };
}
```

Verify after writing:
- 2-space indentation
- Double quotes for all strings
- Trailing commas in all arrays and objects
- Named exports only (no `export default`)
- Relative import "./types" (not "@/data/types")
- File-level block comment with ASCII art header matching foods.ts/remedies.ts style
- Section dividers using `// ── Name ──` pattern with Unicode em-dash
- All 3 presets have macroRatios summing to exactly 100:
  - taiwan-hpa: 12 + 25 + 63 = 100 ✓
  - usda-amdr: 20 + 30 + 50 = 100 ✓
  - japan-mhlw: 16 + 25 + 59 = 100 ✓
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "id:" src/data/dietary-guidelines.ts` returns at least 3 (one per preset)
    - `grep -n "taiwan-hpa\|usda-amdr\|japan-mhlw" src/data/dietary-guidelines.ts` returns 3 lines
    - `grep -n "GUIDELINES" src/data/dietary-guidelines.ts` returns the array export
    - `grep -n "GUIDELINE_MAP" src/data/dietary-guidelines.ts` returns the Map export
    - `grep -n "calculateMacroGrams" src/data/dietary-guidelines.ts` returns the function export
    - `grep -n "export default" src/data/dietary-guidelines.ts` returns nothing
    - `grep -n "@/" src/data/dietary-guidelines.ts` returns nothing (no path alias)
    - Taiwan preset sums: 12 + 25 + 63 = 100 (verify by reading the file)
    - USDA preset sums: 20 + 30 + 50 = 100 (verify by reading the file)
    - Japan preset sums: 16 + 25 + 59 = 100 (verify by reading the file)
    - `npx tsc --noEmit` exits with code 0
    - Taiwan HPA preset contains a comment noting MEDIUM confidence and the v1.1 verification note
  </acceptance_criteria>
  <done>src/data/dietary-guidelines.ts exports GUIDELINES (3 presets), GUIDELINE_MAP, and calculateMacroGrams. All macro ratios sum to 100. TypeScript strict mode compiles cleanly. Taiwan HPA confidence caveat is documented in code.</done>
</task>

</tasks>

<verification>
After the task:
- `cd /home/ubuntu/works/eat-manager && npm run build` — full build including tsc type-check passes
- `grep -n "GuidelinePreset\|MacroGrams" src/data/dietary-guidelines.ts` — both types imported
- `grep -c "{" src/data/dietary-guidelines.ts` — at least 3 opening braces for preset objects
- All 3 preset ids present: taiwan-hpa, usda-amdr, japan-mhlw
</verification>

<success_criteria>
- GUIDELINES array exports exactly 3 GuidelinePreset objects
- All 3 presets have id, name (zh-TW), authority (zh-TW), sourceUrl, year, macroRatios
- macroRatios for each preset sums to exactly 100
- GUIDELINE_MAP provides O(1) lookup for all 3 ids
- calculateMacroGrams(2000, usda_preset) returns { protein: 100, fat: 67, carb: 250 }
- npm run build passes with zero TypeScript errors and zero Vite build errors
- No default exports, no path aliases, no new npm packages added
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-data-foundation/01-02-SUMMARY.md`
</output>
