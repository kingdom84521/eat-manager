---
phase: 12-unified-daily-plan
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - src/App.tsx
  - src/components/SidebarDrawer.tsx
autonomous: false

requirements: [PLAN-01, PLAN-05]

must_haves:
  truths:
    - "Route /plan renders UnifiedPlan instead of DailyPlan"
    - "Route /track redirects to /plan"
    - "Route /items redirects to /plan"
    - "Sidebar 營養補充 nav item still links to /supplements (SupplementManager CRUD)"
    - "DailyPlan.tsx, SupplementSchedule.tsx, NutritionTracker.tsx are deleted"
    - "App builds and deploys successfully"
  artifacts:
    - path: "src/App.tsx"
      provides: "Updated routes with UnifiedPlan and redirects"
      contains: "UnifiedPlan"
    - path: "src/components/SidebarDrawer.tsx"
      provides: "Updated nav items (no changes needed if /supplements stays)"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/UnifiedPlan.tsx"
      via: "import and Route element"
      pattern: "import UnifiedPlan"
---

<objective>
Wire UnifiedPlan into the app router, add redirects for retired routes, and clean up deleted pages.

Purpose: Complete the page consolidation by making UnifiedPlan the active `/plan` route and ensuring old routes redirect properly.
Output: Updated App.tsx with new routing, deleted old page files, passing build
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-unified-daily-plan/12-CONTEXT.md
@.planning/phases/12-unified-daily-plan/12-01-SUMMARY.md

@src/App.tsx
@src/components/SidebarDrawer.tsx

<interfaces>
<!-- From Plan 01 output -->
From src/pages/UnifiedPlan.tsx:
```typescript
export default function UnifiedPlan(): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update App.tsx routes and delete old pages</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx
    - src/pages/UnifiedPlan.tsx
    - src/components/SidebarDrawer.tsx
  </read_first>
  <action>
Update `src/App.tsx` with the following changes:

1. **Replace imports** (lines 3-6):
   - Remove: `import DailyPlan from "./pages/DailyPlan";`
   - Remove: `import NutritionTracker from "./pages/NutritionTracker";`
   - Remove: `import SupplementSchedule from "./pages/SupplementSchedule";`
   - Add: `import UnifiedPlan from "./pages/UnifiedPlan";`

2. **Update Routes** (lines 88-98):
   - Change: `<Route path="/plan" element={<DailyPlan />} />` to `<Route path="/plan" element={<UnifiedPlan />} />`
   - Change: `<Route path="/track" element={<NutritionTracker />} />` to `<Route path="/track" element={<Navigate to="/plan" replace />} />` (per D-19)
   - Change: `<Route path="/items" element={<SupplementSchedule />} />` to `<Route path="/items" element={<Navigate to="/plan" replace />} />` (per D-18 intent -- `/items` was the schedule route, not `/supplements`)
   - Keep: `<Route path="/supplements" element={<SupplementManager />} />` UNCHANGED (per RESEARCH.md Pitfall 2 -- this is the supplement CRUD manager, NOT the schedule)

3. **Delete old page files** (per D-17/D-18/D-19):
   - Delete `src/pages/DailyPlan.tsx`
   - Delete `src/pages/SupplementSchedule.tsx`
   - Delete `src/pages/NutritionTracker.tsx`

4. **SidebarDrawer.tsx**: Review but do NOT change. The "營養補充" nav item links to `/supplements` which is `SupplementManager` (the item CRUD list). This is correct -- users still need access to manage their supplement catalog. The supplement routine is now part of `/plan` (UnifiedPlan). No sidebar changes needed.

CRITICAL per RESEARCH.md Pitfall 2: Do NOT redirect `/supplements` to `/plan`. The `/supplements` route serves SupplementManager (supplement CRUD), which must remain accessible.
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx contains `import UnifiedPlan from "./pages/UnifiedPlan"`
    - App.tsx does NOT contain `import DailyPlan`
    - App.tsx does NOT contain `import NutritionTracker`
    - App.tsx does NOT contain `import SupplementSchedule`
    - App.tsx contains `path="/plan" element={<UnifiedPlan`
    - App.tsx contains `path="/track" element={<Navigate to="/plan"`
    - App.tsx contains `path="/items" element={<Navigate to="/plan"`
    - App.tsx contains `path="/supplements" element={<SupplementManager`
    - src/pages/DailyPlan.tsx does NOT exist
    - src/pages/SupplementSchedule.tsx does NOT exist
    - src/pages/NutritionTracker.tsx does NOT exist
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>App.tsx routes updated, old pages deleted, build passes, /supplements still serves SupplementManager</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify unified daily plan in browser</name>
  <what-built>
    Merged food plan + supplement routine + nutrition budget bar into a single /plan page with checkbox logging, lock mechanic, and single-item swap.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open the app in browser
    2. Navigate to /plan (Today's Plan) -- should show the unified page
    3. Click "產生今日方案" -- food slots should appear (if SCHEDULE has data) with checkboxes
    4. Check a food item -- checkbox shows checkmark, item dims slightly, nutrition bar updates
    5. Verify the "重新產生" button is now disabled (grayed out) while any item is checked
    6. Try the 🔄 swap button on an unchecked item -- it should swap; checked items should NOT have the swap button
    7. Uncheck the food item -- it should revert, nutrition bar updates back
    8. Scroll down to supplement section -- supplements should show with three-state toggle (tap: untouched → taken → skipped → untouched)
    9. Reload the page -- checked state and plan should survive the reload
    10. Navigate to /track -- should redirect to /plan
    11. Navigate to /items -- should redirect to /plan
    12. Open sidebar, tap "營養補充" -- should go to /supplements (SupplementManager, NOT redirect to /plan)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` passes -- production build succeeds
2. Old pages deleted: `ls src/pages/DailyPlan.tsx src/pages/SupplementSchedule.tsx src/pages/NutritionTracker.tsx` all return "No such file"
3. Routes work: /plan shows UnifiedPlan, /track and /items redirect to /plan, /supplements shows SupplementManager
</verification>

<success_criteria>
- UnifiedPlan is the active /plan route
- Old routes (/track, /items) redirect to /plan
- /supplements still serves SupplementManager (not redirected)
- Old page files deleted
- Production build passes
- User verifies unified plan works in browser
</success_criteria>

<output>
After completion, create `.planning/phases/12-unified-daily-plan/12-02-SUMMARY.md`
</output>
