---
phase: 15-menu-composition-editor
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/pages/MyMenu.tsx
autonomous: false
requirements:
  - MENU-05
  - MENU-06
  - MENU-07

must_haves:
  truths:
    - "User taps '新增菜單' in list view and enters editor with empty slots"
    - "User taps edit icon on existing menu card and enters editor pre-populated with saved foods"
    - "Food picker panel slides up when user taps '+' on a slot"
    - "User can search foods by name in the picker"
    - "User can filter foods by tag chips derived from actual food data"
    - "Selecting a food adds it to the active slot and closes the picker"
    - "Saved menu appears in the list after save"
  artifacts:
    - path: "src/pages/MyMenu.tsx"
      provides: "FoodPickerPanel sub-component, create/edit entry points in list view"
      contains: "FoodPickerPanel"
  key_links:
    - from: "src/pages/MyMenu.tsx (FoodPickerPanel)"
      to: "src/pages/MyMenu.tsx (MenuEditor)"
      via: "handleAddFood callback, activeSlotIdx state"
      pattern: "handleAddFood\\(|activeSlotIdx"
    - from: "src/pages/MyMenu.tsx (list view)"
      to: "src/pages/MyMenu.tsx (editor view)"
      via: "setView('editor') on create/edit actions"
      pattern: "setView\\(\"editor\"\\)"
---

<objective>
Build the FoodPickerPanel with search and tag filtering, wire it into the MenuEditor, and add create/edit entry points to the list view — completing the full menu composition flow.

Purpose: Delivers the interactive food selection experience and connects create/edit actions to the editor built in Plan 01.
Output: Complete menu composition editor with food picker, search, tag filter, and create/edit entry points.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-menu-composition-editor/15-CONTEXT.md
@.planning/phases/15-menu-composition-editor/15-RESEARCH.md
@.planning/phases/15-menu-composition-editor/15-01-SUMMARY.md

@src/pages/MyMenu.tsx
@src/data/types.ts
@src/data/foods.ts
@src/lib/item-service.ts
@src/pages/FoodManager.tsx

<interfaces>
<!-- Key types from Plan 01 output that executor needs -->

From src/pages/MyMenu.tsx (after Plan 01):
```typescript
type ViewState = "list" | "editor";
// MenuEditor sub-component exists with:
//   - activeSlotIdx: number | null (which slot's picker is open)
//   - handleAddFood(foodId: string): void (adds food to active slot, closes picker)
//   - allFoods: FoodItem[] (merged FOODS + user foods from ItemService)
//   - foodMap: Map<string, FoodItem> (lookup by id)
```

From src/data/types.ts:
```typescript
export type HealthTag = "insulin_resistance" | "inflammation" | "dehumidify" | ...;
export const HEALTH_TAG_LABELS: Record<HealthTag, string>;
export const HEALTH_TAG_COLORS: Record<HealthTag, string>;
export interface FoodItem {
  id: string; type: "food"; name: string; serving: string;
  cal: number; protein: number; fat: number; carbs: number;
  tags?: HealthTag[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build FoodPickerPanel and wire into MenuEditor</name>
  <files>src/pages/MyMenu.tsx</files>
  <read_first>
    - src/pages/MyMenu.tsx (current state after Plan 01 — has MenuEditor, activeSlotIdx, handleAddFood, allFoods)
    - src/pages/FoodManager.tsx (tag chip pattern — search for "availableTags" and "activeTags" and tag toggle logic)
    - src/data/types.ts (HealthTag type, HEALTH_TAG_LABELS, HEALTH_TAG_COLORS)
    - src/components/SidebarDrawer.tsx (z-index usage — confirm sidebar uses z-50)
  </read_first>
  <action>
Add the FoodPickerPanel as an inline sub-component inside MenuEditor in MyMenu.tsx. This panel slides up when `activeSlotIdx !== null` and lets the user search/filter/select a food (per D-03, D-04, D-05, D-06, D-07).

**1. Add state variables inside MenuEditor** (if not already present from Plan 01):
```typescript
const [searchQuery, setSearchQuery] = useState("");
const [activeTags, setActiveTags] = useState<HealthTag[]>([]);
```

Add import for `HealthTag` type:
```typescript
import type { FoodItem, HealthTag } from "../data/types";
```

Add import for tag display constants:
```typescript
import { HEALTH_TAG_LABELS, HEALTH_TAG_COLORS } from "../data/types";
```

**2. Compute data-derived tag list (per D-07 — NEVER hardcode):**
```typescript
const availableTags = useMemo(() => {
  const tagSet = new Set<HealthTag>();
  allFoods.forEach((f) => (f.tags ?? []).forEach((t) => tagSet.add(t)));
  return [...tagSet].sort();
}, [allFoods]);
```

**3. Compute filtered food list (per D-05, D-06, D-07):**

User-created foods first (per D-05). Combined search (case-insensitive substring) + tag AND filter:
```typescript
const filteredFoods = useMemo(() => {
  let result = [...allFoods].sort((a, b) => {
    // User-created foods (not in static FOODS) appear first
    const aIsUser = !FOOD_MAP.has(a.id) ? 0 : 1;
    const bIsUser = !FOOD_MAP.has(b.id) ? 0 : 1;
    return aIsUser - bIsUser;
  });
  if (searchQuery.trim()) {
    const q = searchQuery.toLowerCase();
    result = result.filter((f) => f.name.toLowerCase().includes(q));
  }
  if (activeTags.length > 0) {
    result = result.filter((f) =>
      activeTags.every((tag) => f.tags?.includes(tag))
    );
  }
  return result;
}, [allFoods, searchQuery, activeTags]);
```

Import `FOOD_MAP` from `../data/foods` (should already be imported or add it).

**4. Tag toggle function:**
```typescript
function toggleTag(tag: HealthTag) {
  setActiveTags((prev) =>
    prev.includes(tag) ? prev.filter((t) => t !== tag) : [...prev, tag]
  );
}
```

**5. Clear picker state when closing:**

Modify `handleAddFood` (already exists from Plan 01) to also clear search/tags:
```typescript
function handleAddFood(foodId: string) {
  if (activeSlotIdx === null) return;
  setSlotFoodIds(prev => prev.map((ids, i) =>
    i === activeSlotIdx ? [...ids, foodId] : ids
  ));
  setActiveSlotIdx(null);
  setSearchQuery("");
  setActiveTags([]);
}
```

Add a close-picker function:
```typescript
function closePicker() {
  setActiveSlotIdx(null);
  setSearchQuery("");
  setActiveTags([]);
}
```

**6. Render FoodPickerPanel at the bottom of MenuEditor's return JSX (per D-04):**

Backdrop (closes picker on tap):
```html
{activeSlotIdx !== null && (
  <div
    className="fixed inset-0 bg-black/30 z-30"
    onClick={closePicker}
  />
)}
```

Panel (slide-up, NOT a headlessui Dialog):
```html
<div
  className={`fixed inset-x-0 bottom-0 z-40 bg-slate-900 border-t border-slate-700 rounded-t-2xl
    transition-transform duration-300 ease-in-out max-h-[70vh] flex flex-col
    ${activeSlotIdx !== null ? "translate-y-0" : "translate-y-full pointer-events-none"}`}
>
  {/* Drag handle */}
  <div className="w-10 h-1 bg-slate-600 rounded-full mx-auto mt-3 mb-2" />

  {/* Header with slot name */}
  <div className="px-4 pb-2 flex items-center justify-between">
    <span className="text-slate-200 font-medium text-sm">
      {activeSlotIdx !== null ? SCHEDULE[activeSlotIdx]?.label ?? "選擇食物" : "選擇食物"}
    </span>
    <button onClick={closePicker} className="text-slate-400 hover:text-slate-200 text-sm">關閉</button>
  </div>

  {/* Search field (per D-06) */}
  <div className="px-4 pb-2">
    <input
      type="text"
      value={searchQuery}
      onChange={(e) => setSearchQuery(e.target.value)}
      placeholder="搜尋食物..."
      className="w-full px-3 py-2 rounded-lg bg-slate-800 border border-slate-600 text-slate-100 text-sm focus:outline-none focus:border-blue-500"
    />
  </div>

  {/* Tag filter chips (per D-07) */}
  {availableTags.length > 0 && (
    <div className="px-4 pb-2 flex flex-wrap gap-1.5">
      {availableTags.map((tag) => (
        <button
          key={tag}
          onClick={() => toggleTag(tag)}
          className={`px-2.5 py-1 rounded-full text-xs font-medium transition ${
            activeTags.includes(tag)
              ? "text-white"
              : "text-slate-300 bg-slate-800 hover:bg-slate-700"
          }`}
          style={activeTags.includes(tag) ? {
            backgroundColor: HEALTH_TAG_COLORS[tag],
          } : undefined}
        >
          {HEALTH_TAG_LABELS[tag]}
        </button>
      ))}
    </div>
  )}

  {/* Scrollable food list */}
  <div className="flex-1 overflow-y-auto px-4 pb-4">
    {filteredFoods.length === 0 ? (
      <p className="text-center text-slate-500 py-8 text-sm">找不到符合的食物</p>
    ) : (
      filteredFoods.map((food) => (
        <button
          key={food.id}
          onClick={() => handleAddFood(food.id)}
          className="w-full flex items-center justify-between py-3 border-b border-slate-700/30 last:border-0 text-left hover:bg-slate-800/50 transition"
        >
          <div>
            <span className="text-slate-100 text-sm">{food.name}</span>
            <span className="text-slate-500 text-xs ml-2">{food.serving}</span>
          </div>
          <span className="text-slate-400 text-xs">{food.cal} kcal</span>
        </button>
      ))
    )}
  </div>
</div>
```

**7. Ensure the MenuEditor return wraps both the editor content AND the picker in a single parent.** The editor's outermost element should be a fragment or div that contains:
  - The sticky totals bar
  - The slot cards area
  - The backdrop div (conditional)
  - The picker panel div

Do NOT use a headlessui Dialog for the picker (per D-04). Do NOT hardcode tag lists (per D-07 and project feedback).
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -20 && grep -c "FoodPickerPanel\|搜尋食物\|availableTags\|filteredFoods\|toggleTag\|closePicker" src/pages/MyMenu.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - src/pages/MyMenu.tsx contains `searchQuery` state variable
    - src/pages/MyMenu.tsx contains `activeTags` state variable typed as `HealthTag[]`
    - src/pages/MyMenu.tsx contains `availableTags` computed via `useMemo` with `new Set<HealthTag>()`
    - src/pages/MyMenu.tsx contains `filteredFoods` computed via `useMemo`
    - src/pages/MyMenu.tsx contains `toggleTag` function
    - src/pages/MyMenu.tsx contains `closePicker` function
    - src/pages/MyMenu.tsx contains `搜尋食物` placeholder text in search input
    - src/pages/MyMenu.tsx contains `HEALTH_TAG_LABELS[tag]` for chip labels
    - src/pages/MyMenu.tsx contains `HEALTH_TAG_COLORS[tag]` for chip colors
    - src/pages/MyMenu.tsx contains `translate-y-full` and `translate-y-0` for slide animation
    - src/pages/MyMenu.tsx contains `fixed inset-x-0 bottom-0` for panel positioning
    - src/pages/MyMenu.tsx does NOT contain `Dialog` import from headlessui for the picker (existing Dialog usage for confirm modals is fine)
    - src/pages/MyMenu.tsx contains `activeTags.every` for AND filter semantics
    - src/pages/MyMenu.tsx contains `找不到符合的食物` empty state message
  </acceptance_criteria>
  <done>FoodPickerPanel slides up from bottom when user taps "+" on a slot, shows search field, data-derived tag chips, and filterable food list. Selecting a food adds it to the active slot and closes the picker.</done>
</task>

<task type="auto">
  <name>Task 2: Add create/edit entry points in list view</name>
  <files>src/pages/MyMenu.tsx</files>
  <read_first>
    - src/pages/MyMenu.tsx (current state after Task 1 — has ViewState, MenuEditor, FoodPickerPanel)
  </read_first>
  <action>
Add the "新增菜單" create button and edit icons to the list view in MyMenu.tsx (per D-10, D-11).

**1. Add "新增菜單" button (per D-10):**

In the list view section of MyMenu's render (the `return` block that shows when `view === "list"`), add a create button right after the `<h1>` heading:

```html
<div className="flex items-center justify-between mb-4">
  <h1 className="text-xl font-bold text-slate-100">我的菜單</h1>
  <button
    onClick={() => { setEditingPreset(null); setView("editor"); }}
    className="px-3 py-1.5 rounded-lg bg-blue-600 hover:bg-blue-500 text-white text-sm font-bold transition"
  >
    + 新增菜單
  </button>
</div>
```

Remove the existing standalone `<h1>` that this replaces.

**2. Add edit icon to each menu card (per D-11):**

In the action buttons area of each menu card (where rename and delete buttons are), add an edit button BEFORE the rename button:

```html
<button
  onClick={() => { setEditingPreset(preset); setView("editor"); }}
  className="p-2 rounded-lg text-slate-400 hover:text-blue-400 hover:bg-slate-700 transition text-base"
  title="編輯"
>
  📝
</button>
```

This opens the editor pre-populated with the selected preset's food assignments (per D-11). The `editingPreset` state is passed to `MenuEditor` as the `preset` prop which initializes `slotFoodIds` from `preset.foodItemIds`.

**3. Update the empty state message** to mention the create button:
```html
<p className="text-sm mt-1">點擊上方「+ 新增菜單」建立你的第一份菜單</p>
```

Replace the existing "從今日方案儲存你的第一份菜單" text.

**4. Verify the full flow works:** After these changes:
- Tapping "新增菜單" should set `editingPreset = null` and `view = "editor"`, rendering MenuEditor with empty slots
- Tapping the edit icon on a card should set `editingPreset = preset` and `view = "editor"`, rendering MenuEditor pre-populated
- The existing load, rename, and delete actions on cards should continue working unchanged
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -20 && npm run build 2>&1 | tail -10 && grep "新增菜單" src/pages/MyMenu.tsx && grep "setEditingPreset" src/pages/MyMenu.tsx | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with zero errors
    - `npm run build` completes successfully
    - src/pages/MyMenu.tsx contains `+ 新增菜單` button text
    - src/pages/MyMenu.tsx contains `setEditingPreset(null); setView("editor")` for create flow
    - src/pages/MyMenu.tsx contains `setEditingPreset(preset); setView("editor")` for edit flow
    - src/pages/MyMenu.tsx contains a 📝 edit icon button on each menu card
    - src/pages/MyMenu.tsx contains `點擊上方` in the updated empty state message
    - Existing rename (✏️) and delete (🗑️) buttons are still present and unchanged
    - Existing headlessui Dialog imports for load/delete confirm modals are preserved
  </acceptance_criteria>
  <done>List view has "新增菜單" button for creating new menus and edit icons on each card for editing existing menus. Both correctly wire into the editor ViewState with appropriate preset (null for create, preset for edit).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full menu composition flow</name>
  <what-built>Complete menu composition editor: create new menu, edit existing menu, food picker with search and tag filter, live nutritional totals, save/persist changes.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:5173/eat-manager/#/menu
    2. Tap "新增菜單" — verify editor opens with empty slots and zero totals
    3. Tap a slot to expand it, tap "+ 新增食物" — verify food picker panel slides up from bottom
    4. Type a food name in the search field — verify list filters in real time
    5. If tag chips appear, tap one — verify food list filters by tag
    6. Tap a food item — verify it's added to the expanded slot and picker closes
    7. Verify the sticky totals bar updates with the food's cal/protein/fat/carbs
    8. Add foods to multiple slots, then tap "儲存" — verify you return to list view and the new menu appears
    9. Tap the 📝 edit icon on the new menu — verify editor opens pre-populated with saved foods
    10. Remove a food from a slot and add a different one, then save — verify changes persist
    11. Navigate away and back to /menu — verify the edited menu still shows updated food count
    12. Verify sidebar drawer still opens normally (no Dialog conflict with picker)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds (TypeScript + Vite production build)
- Full create flow: list → editor (empty) → pick foods → save → list (menu appears)
- Full edit flow: list → editor (pre-populated) → modify foods → save → list (updated)
- Food picker: search filters by name, tag chips filter by tag (AND), user foods appear first
- Nutritional totals update live as foods are added/removed
- Picker is a plain div with translate-y transition, NOT a headlessui Dialog
- Sidebar drawer opens without conflict when picker is closed
- SCHEDULE empty state handled gracefully
</verification>

<success_criteria>
- User can create a new menu from scratch by selecting food items per time slot (MENU-05)
- User can open an existing menu and add/remove food items per slot (MENU-06)
- User can search and filter the food list when picking items for a menu slot (MENU-07)
- Saved changes persist across page navigation and reload (MENU-09 wiring verification)
- All code compiles under TypeScript strict mode
- Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/15-menu-composition-editor/15-02-SUMMARY.md`
</output>
