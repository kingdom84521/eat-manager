---
phase: quick
plan: 260330-6bd
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/Settings.tsx
autonomous: true
must_haves:
  truths:
    - "User can tap a help button in the Google Sheets section to open a dialog"
    - "Dialog explains step-by-step how to set up GAS Web App and obtain the URL + Sheet ID"
    - "All dialog text is in Traditional Chinese (zh-TW)"
    - "Dialog can be dismissed by tapping a close button or outside the dialog"
  artifacts:
    - path: "src/pages/Settings.tsx"
      provides: "Help dialog component and trigger button in Sheets section"
      contains: "showSheetsHelp"
  key_links: []
---

<objective>
Add a help dialog to the Settings page's "Google Sheets 連接" section that explains how to connect Google Sheets step-by-step.

Purpose: Users currently see GAS URL and Sheet ID fields with no guidance on how to obtain these values. A help dialog provides inline instructions so users can self-serve the setup.
Output: A modal/dialog triggered by a help button next to the section header, with step-by-step Traditional Chinese instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/pages/Settings.tsx
@scripts/gas-api.js (first 12 lines — setup steps reference)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Google Sheets help dialog to Settings page</name>
  <files>src/pages/Settings.tsx</files>
  <action>
In `src/pages/Settings.tsx`, add:

1. A `showSheetsHelp` boolean state (`useState(false)`).

2. A help trigger button: Add a "?" or info icon button next to the "Google Sheets 連接" h2 header. Style it as a small circular button (`w-6 h-6 rounded-full bg-slate-700 text-slate-400 hover:bg-slate-600 hover:text-white text-xs font-bold`) that calls `setShowSheetsHelp(true)`.

3. A modal dialog component rendered conditionally when `showSheetsHelp` is true. The dialog should:
   - Use a fixed overlay (`fixed inset-0 z-50 flex items-center justify-center bg-black/60`) for the backdrop
   - Clicking the backdrop closes the dialog
   - Inner content panel: `bg-slate-800 rounded-2xl p-5 mx-4 max-w-md max-h-[80vh] overflow-y-auto`
   - Title: "如何連接 Google Sheets"
   - A close button (X) in the top-right corner of the panel

4. Dialog content — step-by-step instructions in Traditional Chinese:

   **步驟 1：建立 Google 試算表**
   "開啟 Google Sheets，建立一個新的試算表（或使用現有的）。記下網址列中的 Sheet ID — 它是 `https://docs.google.com/spreadsheets/d/` 後面那段長字串。"

   **步驟 2：開啟 Apps Script 編輯器**
   "在試算表中，點選「擴充功能」>「Apps Script」，這會開啟 Apps Script 編輯器。"

   **步驟 3：貼上 API 程式碼**
   "將本專案提供的 `gas-api.js` 程式碼完整貼入編輯器中，取代預設內容，然後儲存。"

   **步驟 4：部署為 Web App**
   "點選「部署」>「新增部署」>「網頁應用程式」。設定：" followed by a styled list:
   - 「執行身分」→ 我
   - 「誰可以存取」→ 任何人

   **步驟 5：複製網址**
   "部署完成後，複製產生的 Web App 網址，貼到下方「GAS 網址」欄位。將 Sheet ID 貼到「Sheet ID」欄位。"

   Optionally add a small note at bottom: "提示：每次修改程式碼後需重新部署才會生效。"

5. Style each step consistently:
   - Step number: `text-blue-400 font-bold text-sm mb-1`
   - Step content: `text-slate-300 text-sm mb-4 leading-relaxed`
   - Use `<ol>` or numbered divs — whichever fits the existing code style better (the file uses divs, so prefer divs)

Keep the dialog as an inline sub-component within Settings.tsx (following the existing pattern of sub-components in page files). Do NOT extract to a separate file — this matches the codebase convention.
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npm run build</automated>
  </verify>
  <done>
    - Help button visible next to "Google Sheets 連接" header
    - Clicking the button opens a modal with 5 step-by-step instructions in Traditional Chinese
    - Clicking backdrop or X button closes the dialog
    - Build passes with no TypeScript errors
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes without errors
- Settings page renders without console errors
- Help dialog opens/closes correctly
</verification>

<success_criteria>
- A "?" button appears in the Google Sheets section header
- Tapping it shows a scrollable modal with 5 setup steps in Traditional Chinese
- The dialog can be closed via X button or backdrop click
- No TypeScript or build errors
</success_criteria>

<output>
After completion, create `.planning/quick/260330-6bd-add-google-sheets-connection-help-dialog/260330-6bd-SUMMARY.md`
</output>
