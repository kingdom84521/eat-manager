---
phase: quick
plan: 260330-88s
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/lib/settings-service.ts
  - src/pages/Settings.tsx
  - src/data/bmr.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "User sees a birthday input field instead of age number input"
    - "Tapping the birthday field expands an inline calendar block below it"
    - "Selecting a date on the calendar sets the birthday and auto-computes age for BMR"
    - "TDEE live preview updates correctly when birthday changes"
    - "Existing users with ageYears data are migrated to birthday on load"
  artifacts:
    - path: "src/data/types.ts"
      provides: "UserProfile with birthday: string instead of ageYears: number"
      contains: "birthday: string"
    - path: "src/lib/settings-service.ts"
      provides: "Version 2 migration from ageYears to birthday"
      contains: "settings_version: 2"
    - path: "src/pages/Settings.tsx"
      provides: "Inline calendar date picker component and birthday form field"
    - path: "src/data/bmr.ts"
      provides: "BMR functions unchanged — still accept ageYears as number param"
  key_links:
    - from: "src/pages/Settings.tsx"
      to: "src/data/bmr.ts"
      via: "Computes age from birthday string, passes integer age to calculateBMRResult"
      pattern: "calculateBMRResult\\("
    - from: "src/lib/settings-service.ts"
      to: "src/data/types.ts"
      via: "UserProfile type with birthday field"
      pattern: "profile\\.birthday"
---

<objective>
Replace the age number input in Settings with a birthday date picker that uses an inline calendar component (expanding below the input when focused). Age is computed from birthday for BMR calculation. Includes schema migration from version 1 (ageYears) to version 2 (birthday).

Purpose: Better UX — birthday is entered once and age auto-updates; removes ambiguity of manually entering age.
Output: Updated Settings page with inline calendar, migrated schema, all BMR calculations still working.
</objective>

<execution_context>
@.planning/quick/260330-88s-replace-age-input-with-birthday-date-pic/260330-88s-PLAN.md
</execution_context>

<context>
@src/data/types.ts
@src/data/bmr.ts
@src/lib/settings-service.ts
@src/pages/Settings.tsx

<interfaces>
From src/data/types.ts:
```typescript
export interface UserProfile {
  ageYears: number;          // CHANGE TO: birthday: string (ISO date "YYYY-MM-DD")
  sex: "male" | "female";
  heightCm: number;
  weightKg: number;
  activityLevelId: ActivityLevelId;
}
```

From src/data/bmr.ts:
```typescript
// These functions keep ageYears as parameter — do NOT change their signatures.
// The caller (Settings.tsx) computes age from birthday and passes it in.
export function calculateBMR(ageYears: number, sex: "male" | "female", heightCm: number, weightKg: number): number;
export function calculateBMRResult(ageYears: number, sex: "male" | "female", heightCm: number, weightKg: number, activityLevelId: ActivityLevelId): BMRResult;
```

From src/lib/settings-service.ts:
```typescript
export const SettingsService = {
  getUserProfile(): UserProfile | null;
  getComputedTargets(): { tdee: number; macros: MacroGrams } | null;
  saveUserProfile(profile: UserProfile): void;
  // ...
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update UserProfile type and SettingsService migration</name>
  <files>src/data/types.ts, src/lib/settings-service.ts</files>
  <action>
1. In `src/data/types.ts`, change `UserProfile.ageYears: number` to `birthday: string` with JSDoc comment `/** 生日（ISO 格式 YYYY-MM-DD） */`. Do NOT add an ageYears field — age is computed on demand from birthday.

2. In `src/lib/settings-service.ts`:
   - Change `defaultSettings()` to return `settings_version: 2`.
   - Add migration case in `migrate()`: when `settings_version === 1`, convert `userProfile.ageYears` to an approximate `birthday` by computing `new Date().getFullYear() - ageYears` and setting birthday to `"YYYY-01-01"` (Jan 1 of the birth year, since we don't know exact date). Then set `settings_version = 2` and fall through.
   - Update `getComputedTargets()` to compute age from `profile.birthday` before passing to `calculateBMRResult`. Use a helper: `function computeAgeFromBirthday(birthday: string): number` that calculates full years elapsed (accounting for whether birthday has occurred this year). Clamp result to 10-120 range.
   - Export `computeAgeFromBirthday` so Settings.tsx can reuse it.

3. Keep `calculateBMR` and `calculateBMRResult` in `src/data/bmr.ts` completely unchanged — they still accept `ageYears: number` as a parameter. The conversion from birthday to age happens at the call site.
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>UserProfile uses `birthday: string`, migration converts v1 ageYears to v2 birthday, `computeAgeFromBirthday` is exported and used in getComputedTargets, TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Replace age input with inline calendar date picker in Settings</name>
  <files>src/pages/Settings.tsx</files>
  <action>
1. **Create an inline `BirthdayPicker` component** defined in the same file (following the project's sub-component-in-same-file pattern). This is a pure React/Tailwind calendar — NO external library.

   The component:
   - Props: `value: string` (ISO date or ""), `onChange: (date: string) => void`
   - Shows a text input displaying the selected date in "YYYY / MM / DD" format (or placeholder "選擇生日")
   - When the input is focused/clicked, an inline calendar block renders BELOW the input (not a dropdown/popup — it pushes content down, rendered in normal flow)
   - Calendar shows a month grid with:
     - Header row: month/year display with left/right arrow buttons to navigate months
     - A year selector: clicking the year in the header toggles a scrollable year list (range: current year minus 120 to current year minus 10) for quick year jumping
     - Day-of-week headers: 日 一 二 三 四 五 六
     - Day grid: 6 rows x 7 cols, days from current month shown, previous/next month days shown dimmed
     - Selected day highlighted with `bg-blue-600 text-white rounded-full`
     - Today marked with a subtle ring `ring-1 ring-blue-400`
   - Clicking a day sets the value and collapses the calendar
   - Clicking outside the component collapses the calendar (use a ref + useEffect click-outside handler)
   - All text in Traditional Chinese
   - Dark theme styling consistent with existing: `bg-slate-800` calendar background, `text-slate-300` for days, `text-slate-600` for dimmed days, `hover:bg-slate-700` for day hover
   - Default the calendar view to: if value is set, show that month; if empty, show January of (current year - 30) as a sensible starting point

2. **Update `ProfileFormState`**: Replace `ageYears: string` with `birthday: string` (ISO date string or "").

3. **Update `ProfileErrors`**: Replace `ageYears?: string` with `birthday?: string`.

4. **Update `validateProfile`**: Validate birthday is a valid date string and the computed age is 10-120. Error message: `"請選擇有效的生日（年齡須介於 10–120 歲）"`. Use `computeAgeFromBirthday` imported from `settings-service.ts`.

5. **Update `isProfileComplete`**: Check `form.birthday !== ""` instead of ageYears.

6. **Update `computeTdee`**: Compute age from `form.birthday` using `computeAgeFromBirthday`, then pass to `calculateBMRResult`.

7. **Update `handleProfileChange`**: When saving to SettingsService, pass `{ birthday: next.birthday, ... }` instead of ageYears.

8. **Update initial state**: Load `birthday` from `savedProfile.birthday` (string, not number conversion).

9. **Update the JSX**: Replace the age number `<input>` with `<BirthdayPicker value={form.birthday} onChange={(d) => handleProfileChange({ birthday: d })} />`. Keep the label as `生日` with the same `<label>` styling. Show computed age next to the label or below the picker: `"（{age} 歲）"` when birthday is set.

10. **Show computed age display**: Below the BirthdayPicker (or inline with the label), show the computed age in a subtle text: e.g., `<span className="text-slate-500 text-xs ml-2">（32 歲）</span>` next to the label "生日". This gives the user confirmation their age is correctly derived.
  </action>
  <verify>
    <automated>cd /home/ubuntu/works/eat-manager && npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>Age number input is replaced with inline calendar birthday picker. Selecting a birthday auto-computes age for TDEE. Calendar expands as an inline block (not dropdown). Existing saved profiles load correctly via migration. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero TypeScript errors
2. Run dev server (`npm run dev`), navigate to Settings page:
   - Birthday field shows instead of age input
   - Clicking birthday field expands inline calendar below
   - Can navigate months/years, select a date
   - TDEE updates after selecting birthday
   - Computed age shows next to the label
3. Test migration: In browser console, set localStorage `eat_manager_settings` to a v1 object with `ageYears: 30`, reload — should show birthday as "1996-01-01" (approximate) and TDEE should compute correctly
</verification>

<success_criteria>
- Birthday date picker replaces age number input
- Calendar is inline (expands as block, not dropdown)
- Age computed from birthday for BMR/TDEE
- Schema migrated from v1 (ageYears) to v2 (birthday)
- All UI text in Traditional Chinese
- No external date picker dependency
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/quick/260330-88s-replace-age-input-with-birthday-date-pic/260330-88s-SUMMARY.md`
</output>
