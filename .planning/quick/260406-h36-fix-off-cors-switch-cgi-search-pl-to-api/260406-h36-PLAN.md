---
quick_id: "260406-h36"
description: "Fix OFF CORS: proxy OFF search through GAS to avoid CORS"
tasks: 2
---

# Quick Task 260406-h36: Fix OFF CORS via GAS Proxy

OFF's `cgi/search.pl` endpoint doesn't return CORS headers (returns 503 error page without Access-Control-Allow-Origin). Fix by proxying OFF searches through GAS.

## Task 1: Add proxyOff action to GAS API

**Files:** `scripts/gas-api.js`
**Action:**
- Add `proxyOff` case to `doGet` switch — takes `query` and `pageSize` params
- Uses `UrlFetchApp.fetch()` to call OFF API v2 and return results
- Bump `API_VERSION` to 3

## Task 2: Update FoodManager to use GAS proxy

**Files:** `src/pages/FoodManager.tsx`
**Action:**
- Replace direct `fetch(world.openfoodfacts.org/cgi/search.pl...)` with call through GAS proxy
- Use existing `getGasUrl()` pattern or sheets-api helper
**Verify:** `npm run build` passes
